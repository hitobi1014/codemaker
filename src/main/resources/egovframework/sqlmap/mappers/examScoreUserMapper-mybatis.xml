<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.codemaker.user.classroom.exam.dao.ExamScoreUserMapper">

<select id="selectAllExamScore" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO" resultType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO">
SELECT
    exam_id,
    es_fscore,
    es_lscore,
    user_id,
    es_edate
FROM
    examscore
WHERE
	user_id = #{userId}
</select>
<insert id="insertExamScore" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO">
INSERT INTO examscore (
    exam_id,
    es_fscore,
    es_lscore,
    user_id,
    es_edate
) VALUES (
    #{examId},
    #{esFscore},
    #{esLscore},
    #{userId},
    #{esEdate}
)
</insert>
<update id="updateExamScore" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO">
UPDATE examscore
    SET
         es_lscore = #{esLscore},
         es_edate = SYSDATE
WHERE
    exam_id = #{examId}
</update>
<select id="selectExamScore" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO" resultType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO">
SELECT
    exam_id,
    es_fscore,
    es_lscore,
    user_id,
    es_edate
FROM
    examscore
WHERE
	exam_id = #{examId}
</select>
</mapper>