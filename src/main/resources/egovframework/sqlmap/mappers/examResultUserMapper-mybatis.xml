<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.codemaker.user.classroom.exam.dao.ExamResultUserMapper">

<select id="selectAllExamResult" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamScoreVO" resultType="kr.co.codemaker.user.classroom.exam.vo.ExamResultVO">
SELECT
    que_id,
    er_answer,
    er_check,
    user_id,
    exam_id
FROM
    examresult
WHERE
	exam_id = #{examId}
</select>
<insert id="insertExamResult" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamResultVO">
INSERT INTO examresult (
    que_id,
    er_answer,
    er_check,
    user_id,
    exam_id
) VALUES (
    #{queId},
    #{erAnswer},
    #{erCheck},
    #{userId},
    #{examId}
)
</insert>
<update id="updateExamResult" parameterType="kr.co.codemaker.user.classroom.exam.vo.ExamResultVO">
UPDATE examresult
       SET 
       	er_answer = #{erAnswer},
    	er_check = #{erCheck}
WHERE
    que_id =  #{queId}
</update>
</mapper>